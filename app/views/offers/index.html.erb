<h1>Localidades com ofertas turísticas</h1>

<ul>
  <% @offers.each do |offer| %>
    <li><%= link_to offer.city, offer_path(offer), class: 'btn btn-primary btn-sm' %></li>
  <% end %>
</ul>

<%= link_to 'Criar oferta de guia', new_offer_path, class: 'btn btn-warning' %>