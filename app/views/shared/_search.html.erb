<%= form_tag search_path, method: :get do  %>
  <%= text_field_tag :query,
    params[:query],
    class: 'form-control',
    placeholder: 'Procure por cidade ou atividade' %>
  <%= submit_tag 'Procurar', class: 'btn btn-primary' %>
<% end %>
